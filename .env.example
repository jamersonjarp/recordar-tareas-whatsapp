# API key de Groq para transcribir audios
GROQ_API_KEY=your_groq_api_key

# Tu numero personal de WhatsApp (el que recibe los recordatorios)
OWNER_NUMBER=573004638234

# (Opcional) URL del frontend en Netlify para CORS
# FRONTEND_URL=https://tu-app.netlify.app

# (Opcional) Para Railway — contenido JSON de firebase-key.json
# FIREBASE_CONFIG={"type":"service_account",...}
